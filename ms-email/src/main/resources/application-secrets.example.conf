parallaxbot {
    email {
        client {
            id     = ""   # Google OAuth client id
            secret = ""   # Google OAuth client secret
        }
        username = ""     # Gmail address used as sender, e.g. youraccount@gmail.com
    }
}
