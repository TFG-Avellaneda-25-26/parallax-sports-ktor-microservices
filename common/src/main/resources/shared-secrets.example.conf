parallaxbot {
    api {
        key = ""    # X-Api-Key for internal service-to-service calls (Spring ↔ Ktor)
    }
    redis {
        url = ""    # redis://host:port  e.g. redis://localhost:6379
    }
}
