include "shared-secrets.conf"
include "shared-secrets.local.conf"

parallaxbot {
    redis {
        stream = "alerts-stream"
    }

    api {
        base-url = "http://localhost:8080"
        endpoints {
            event = ${parallaxbot.api.base-url}"/api/events"
            auth  = ${parallaxbot.api.base-url}"/api/auth"
        }
    }
}

