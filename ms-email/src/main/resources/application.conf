include "shared-data.conf"
include "application-secrets.conf"
include "application-secrets.local.conf"

ktor {
    deployment { port = 8084 }
    application { modules = [es.daw.parallaxbot.email.ApplicationKt.module] }
}

parallaxbot {
    email {
        from = "ParallaxSports"
    }
}
